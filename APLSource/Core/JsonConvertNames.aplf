 JsonConvertNames←{
    ⍝ Converts between APL names and JSON
    ⍝ ⍺: optional flag. 0: convert from JSON to APL name. 1: convert from APL to JSON name
    ⍝ ⍵: vector of JSON or APL names to be converted

     ⍺←'⍙'=⊃¨⊆⍵
     ⊃⍣(1=≡⍵)⊢⍺(7162⌶)¨⊆⍵
 }
