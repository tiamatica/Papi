 r←getComponent content;comppath;ref
 ref←{
     2=⍵.⎕NC'⍙⍙36⍙ref':⍵.⍙⍙36⍙ref
     ⍵.(⍎⊃⎕NL ¯9).schema.⍙⍙36⍙ref
 }content
 comppath←translatePath ref
 :If '#.components'(⊃⍷)comppath
     r←⍎'∆',2↓comppath
 :EndIf

⍝
