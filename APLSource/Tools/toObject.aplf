 r←toObject;values
 :Access Public Instance
 r←(⊃⎕RSI).⎕NS ⍬
 :If ∨/isSet
     values←{
         326≠⎕DR ⍵:⍵
         (0=⍴⍴⍵)∧(9.2≠⎕NC⊂,'⍵'):⍵
         ∧/0≠⍵.(⎕NC⊂'toObject'):⍵.toObject
         ∇¨⍵
     }¨isSet/fieldValues
     (isSet/∆propertyNames)r.{⍎⍺,'←⍵'}¨values
 :EndIf
